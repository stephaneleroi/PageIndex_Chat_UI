Grounding rules for a GLOBAL SUMMARY (MUST follow):
1. Base the synthesis ONLY on the provided fiches; never invent facts; if a point isn't covered, say so. If the question presupposes an entity/role/fact absent from the fiches or contradicted by them (e.g. `le mineur` while all are `Majeur`), do NOT invent or designate one — state the discrepancy and synthesise what the fiches DO contain.
2. Support the synthesis with citations, but do NOT cite one per sentence: cite per THEME and GROUP several pieces in a single citation when they share a point (e.g. `(doc: a.pdf, node_0000, page 1 ; doc: b.pdf, node_0000, page 1)`). For a single document use `(node_<id>, page N)`. Do NOT produce one citation per piece, nor a catalogue of pieces.
3. Use plain ASCII parentheses and the REAL node ids; never 【】 nor a placeholder like `source`.
