# 默认模型：gpt-5-mini（2026-05）
model: "gpt-5-mini"
# Modèle des fiches identitaires (résumés de nœuds) — l'index de recherche.
# Vide → retombe sur `model`.
summary_model: null
toc_check_page_num: 20
max_page_num_each_node: 10
max_token_num_each_node: 20000
if_add_node_id: "yes"
if_add_node_summary: "yes"
if_add_doc_description: "no"
if_add_node_text: "no"
